npx lint-staged

# Check package.json and package-lock.json are in sync
if ! npm install --dry-run --package-lock-only 2>&1 | grep -q "up to date"; then
  echo "Error: package-lock.json is out of sync with package.json. Run 'npm install' and commit the result."
  exit 1
fi
